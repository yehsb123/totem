# 작업 로그

지시사항·결정·상태를 날짜별로 누적한다. 기존 항목은 지우지 않는다.

## 2026-09-23

**지시사항 (예성빈 책임님)**
- 소스 정리: 화면별·기능별·페이지별·API별로, 환경변수까지 맞춰서 꼼꼼하게. 소스 전부 엎어도 됨
- 백엔드도 명확하게 확장·디벨롭
- 로그인 후엔 다른 URL: **메인 화면은 Vercel**, **기능 화면은 GitHub Actions 로 CI/CD**
- DB 는 일단 스키마만 — 모든 화면에 맞춰, 유저 DB 등 명확하게 관리
- 정합성 100% 기반, 오류 있으면 알려줄 것, 백로그 하나하나 설정해서 진행
- 토템 서포트 세션과 업무 분할
- 수정하면 항상 책임님 이름(yehsb123)으로 커밋·푸시 (AI 표시 금지)

**확인한 것**
- 공개 레포에 `totemBE/.env`(DB 접속정보·JWT 비밀키·TourAPI 키) 커밋, `node_modules` 8,776개 커밋
- FE 가 호출하는 API 경로 대부분이 BE 에 없음 → 로그인·코스 저장·투어·리뷰·설정 화면이 실제로 동작하지 않던 상태 (docs/AUDIT.md)

**결정**
- 모노레포 `apps/{web,console,api}` + `packages/shared` (API 계약 단일 출처)
- 도메인이 달라 localStorage 공유 불가 → 1회용 인계 코드(handoff)로 web → console 로그인 전달
- 조직(테넌트) 단위 데이터 격리, soft delete
- 작업 브랜치 `dev`

**진행**: 뼈대 재편 커밋 → PR #1 로 main 머지됨(책임님) → 구 배포 워크플로가 옛 경로로 실패

## 2026-09-25

**진행**
- API TypeScript 재작성 + 테스트 25건 통과, 실제 기동 스모크 테스트 통과
- 콘솔 6개 화면 재작성, 정적 export 빌드(basePath /totem) 통과, lint 경고 0
- 공용 클라이언트로 전 화면 흐름 E2E 17건 통과 (라이브 API)
- 메인(web)은 별도 작업 에이전트가 격리 워크트리에서 진행 (서포트 세션이 종료돼 있어 대체)
- CI(ci.yml) · 콘솔 배포(deploy-console.yml) 교체, ENV/API/DATABASE/DEPLOY 문서

**책임님 확인 필요**
- SEC-002 유출 키 재발급 (MongoDB 비밀번호, JWT, TourAPI, 카카오 REST)
- SEC-003 git 히스토리에서 .env 제거 여부 (force push 필요)
- OPS-004 API 배포처 결정
- 대시보드 관광소비 금액 단위 (원본이 원인지 천원인지 미검증 — 현재 "원" 표시)

## 2026-09-25 (계속)

- 책임님 `npm run dev` 실패(루트에 dev 스크립트 없음) → 루트 `npm run dev` 로 api·web·console 동시 실행 추가
- 로컬 `apps/api/.env` 의 옛 Atlas 주소가 해석되지 않아 api 가 죽던 문제 → 개발 모드에서만 인메모리 DB 로 대체(경고 출력), 운영은 기동 중단 유지. **자격증명 파일은 건드리지 않음**
- 메인(web) 재구성 브랜치 머지 (커밋 9개, 작성자 yehsb123, AI 표기 없음)
- 실제 브라우저 E2E 10/10, UI 결함 3건 수정 (입력칸 너비, 코스메이커 라벨, 통화 중복)

## 2026-09-25 (책임님 부재 — 자율 루프 시작)

**지시사항**: 1박 2일 여행 중 10분마다 루프로 원하는 결과물을 만들 것. 정합성 100%, 필요한 기능은 계속 추가, 묻지 말고 작업.
**진행**: npm audit 정리(vitest 5), 요금제 모델을 가격표와 일치(trial/basic/pro), 투어 일정표 PDF 구현, 투어 등록 시 달력 자동 표시,
인쇄 규칙 버그·달력 접근성 버그 수정, 메인 스크린샷 9장 실제 캡처로 교체, 가이드 문구 교정.
**자율 큐**: docs/BACKLOG.md §6
- AUTO-01 ✅ 브라우저 E2E 13건을 `e2e/`(Playwright)로 편입·CI 잡 추가. 과정에서 `next dev`/`next build` 가 같은 `.next` 를 공유해 빌드 시 개발 서버가 멈추는 결함 발견 → 개발 산출물 `.next-dev` 분리
- AUTO-02 ✅ 코스메이커 "내 코스"(검색·불러오기·일정표·삭제)·"새 코스". 투어가 연결된 코스 삭제는 409 로 막음(투어 링크 깨짐 방지), 목록에 tourCount. API 테스트 26 · E2E 14 (2회 연속 통과)
- AUTO-03 ✅ 코스메이커 "카카오 검색" 탭: 관광정보에 없는 곳을 카카오 로컬 검색으로 찾아 담기(업종 코드→분류, 키 없으면 안내). 같은 장소 중복 판정을 placeId·contentId 둘 다로 확장. API 29 · E2E 15. **실제 카카오 결과 화면은 키 등록 후 확인 필요**
- AUTO-04 ✅ 코스메이커 일차 동선 요약(거리·시간·택시비, 지도에 실제 도로 경로). 호출 한도 고려해 버튼으로만 계산·일정 바뀌면 결과 폐기. 지도 번호·일정표·동선 순서 통일(시간대 → 숙소). API 32 · E2E 16. **실제 경로 화면은 카카오 키 등록 후 확인 필요**
- AUTO-05 ✅ 메인 OG 전용 이미지 og.jpg(1200×630, 92KB, 사이트 톤·한글 Pretendard 정적 렌더). og:image 기준 주소가 localhost 로 박히지 않게 Vercel 운영 도메인 대체. 제목 템플릿 "Totem"→"ToTem" 브랜드 통일
- AUTO-06 ✅ 콘솔 375px 점검: 7개 화면 가로 넘침 측정 → 코스메이커(일정 칸 화면 밖·터치로 드래그 불가) 장소/지도/일정 탭 + [담기] 버튼(알맞은 빈 칸 자동, 데스크톱도 사용), 일정관리(상세·새 일정 버튼이 모바일에서 숨겨짐) 달력 아래 상세·도구줄 버튼, 표 머리글 줄바꿈 방지. E2E 17 (모바일 1건 추가)
- AUTO-07 ✅ 요청 ID(X-Request-Id, 안전한 형식만 이어 씀)·오류 본문 requestId·5xx 화면에 "오류 ID" 표시(web·console 공통 describeApiError)·접근 로그 레벨(5xx error/4xx warn)·userId/orgId. API 37 · E2E 17
- AUTO-08 ✅ 전 화면 정합성 재점검: 계약 자동 대조·인증 적용 범위 테스트 추가(둘 다 돌연변이 시험으로 검출 확인), 과장 문구 5건 교정, 브랜드 ToTem 통일, 문서 수치 교정 — AUDIT §8
  - **책임님 확인 필요**: ① 고객 지원 연락처(이메일·전화·채널) ② 대시보드 통계 갱신 주기·담당(현재 2024-07~2025-06 고정) — 정해지면 FAQ·리소스 반영
- AUTO-09 🔄 브랜치 `chore/next16`: Next 16.3.6 업그레이드(빌드·API 41·E2E 17 통과, postcss 높음 취약점 해소), next lint→ESLint CLI. 새 React hooks 규칙 지적 27 → 22 (web 0, console 모달 5건 해결). 남은 22건 수정 후 dev 머지 예정
- AUTO-09 ✅ Next 16.3.6 업그레이드 dev 머지. React Compiler 기반 hooks 규칙 지적 27건 전부 코드로 해결(끄지 않음): effect 안 동기 setState → 파생 상태·렌더 중 조정·조회 키 기반 로딩, 모달은 열 때 새로 마운트, 렌더 중 ref 접근 → 콜백 ref, 렌더 안 컴포넌트 정의 → 모듈 수준, 다른 출처 이동은 절대 URL. postcss 높음 취약점 해소, CI 에 lint 단계 추가. API 41 · E2E 17
- AUTO-10 ✅ 조직 멤버 초대(링크·7일·1회)·역할 변경·제외·소유권 이전 + 콘솔 설정 "멤버 관리" 탭 + 메인 /invite 수락 페이지. 소유자 탈퇴 안내가 없는 기능을 가리키던 불일치 해소. API 48 · E2E 18. **초대 메일 자동 발송은 메일 서비스 결정 후**(지금은 링크 복사 전달)
  - **책임님 확인 필요**: 초대 메일 발송용 메일 서비스(SES·SendGrid 등) 사용 여부
- AUTO-11 ✅ 삭제된 조직: 요청·로그인 모두 401(방어 한 겹 추가), 대기 초대 취소, 유예 후 영구 삭제(npm run purge, 결제 기록 5년 보존·조직 표지만 남김) + 정책 문서(DATABASE.md). API 51
  - **책임님 확인 필요**: 영구 삭제 유예 기간(기본 30일)·결제 기록 보존 기간(5년 가정)·운영 예약 실행 방법(API 배포처와 함께)
- 재점검(큐 소진 후): 화면 없는 API(places.sync), 잘리는 목록·평균 불일치 발견 → AUTO-12·13 추가
  - **책임님 확인 필요**: 역할별 데이터 권한 — 지금은 멤버도 투어·코스·리뷰를 삭제할 수 있음. 삭제를 관리자 이상으로 제한할지
- AUTO-12 ✅ 설정 > 데이터 관리(관광정보 동기화) + 공용 데이터 보호(전역 간격 429·동시 실행 409·상태 API). 라우트 가로채기 방지, 테스트·E2E 가 로컬 .env 키를 읽던 격리 결함 수정. API 53 · E2E 19 — AUDIT §9
- AUTO-13 ✅ 잘리는 목록·평균 불일치: 리뷰 요약 API(전체 기준), 리뷰·투어 목록 페이지+전체 건수, 일정 폼 투어 선택 검색·상한 안내·연결 투어 누락 표시 수정. API 54 · E2E 19
- 재점검 3차: 로그 토큰 노출·시간 검증·보안 문서 → AUTO-14~16. AUTO-14 ✅ 접근 로그 URL 비밀값 마스킹(실서버 로그 원문 0건 확인). API 56
- AUTO-15 ✅ 시간 검증 강화(24:59·끝<시작 차단), 기존 기본값·시드·입력칸은 영향 없음. API 58 · E2E 19
- AUTO-16 ✅ docs/SECURITY.md (통제 = 코드로 확인한 것만, 근거 위치·테스트 표기, 맞교환 R1~R8). 대조 중 콘솔 next 검증 누락(/\) 발견 → shared sanitizeNext 로 통일. API 59 · E2E 19
  - **책임님 확인 필요**: R1 운영 도메인을 한 상위 도메인으로 묶을지(쿠키 세션 전환 가능), R2 콘솔 호스팅(GitHub Pages 는 CSP 헤더 불가), R4 계정 잠금 정책
- AUTO-17 ✅ 프런트 CSP: 공용 생성기 `@totem/shared/csp`, web 응답 헤더(+X-Frame-Options·nosniff·Referrer-Policy), console `<meta>`(GitHub Pages). connect-src = 자기 자신+API 만 → XSS 시 토큰 외부 유출 차단. 운영 빌드 브라우저 점검: 위반 0건·외부 fetch 차단. AUTO-18 ✅ 중첩 `<main>` 해소. API 59 · E2E 19
  - **책임님 확인 필요**: 카카오 로그인·지도 화면의 CSP 는 키가 없어 실제 로드 확인 못 함 — 키 등록 후 브라우저 콘솔에 "Content Security Policy" 경고가 없는지 확인(있으면 그 출처를 `packages/shared/csp.mjs` 에 추가)
- 재점검 5차(문서↔코드 전수 대조 + 화면 문구) → AUTO-19~22 ✅: 영속 DB 데모 계정 기본 비밀번호 차단(보안), UTC 날짜 표시 3곳, 없는 기능 약속 문구 3건, API·DATABASE·ENV 문서 불일치 12건 — AUDIT §12. API 60 · E2E 19
  - **책임님 확인 필요**: 결제 기록 5년 경과분 자동 삭제를 넣을지(지금은 계속 보존 — 보존 기간 결정과 함께)
- 재점검 6차(역할 경계: API 역할 검사 ↔ 콘솔 노출 조건) → AUTO-23 ✅ 관리자 초대 취소·덮어쓰기는 소유자만, AUTO-24 ✅ 소유권 이전 직후 화면 권한 갱신. API 61 · E2E 20 — AUDIT §13
  - **책임님 확인 필요**: 결제 정보(요금제·결제 수단 끝 4자리·결제 내역)를 멤버도 볼 수 있음 — 관리자 이상으로 제한할지 (앞서 기록한 멤버 삭제 권한과 함께 결정)
- 재점검 7차(세션 만료·오류 상태) → AUTO-25 ✅ 다중 탭 동시 만료 시 재사용 탐지 오탐으로 전 세션 폐기되던 결함·네트워크 오류 시 로그아웃 결함 수정(Web Locks 직렬화, 거절 시에만 로그아웃). 클라이언트 테스트 3건(수정 전 코드로 2건 실패 확인), Chrome 두 탭 점검. API 64 · E2E 20 — AUDIT §14. 빈 상태·저장 중 오류 화면 점검은 다음 회차
- 재점검 7차 마무리 → AUTO-26 ✅ 저장 뒤 목록 갱신 실패가 저장 실패로 보여 중복 생성되던 결함(일정·리뷰), AUTO-27 ✅ 빈 상태 문구 3건. 빈 조직 7개 화면 순회·저장 중 연결 끊김 Chrome 점검. API 64 · E2E 20
- 재점검 8차(입력 한계값: zod ↔ 모델 ↔ 화면) → AUTO-28 ✅ 영어로 나가던 검증 오류 문구 한국어화(화면·API 공통), 아이디 찾기 상한. API 66 · E2E 20 — AUDIT §15
- 재점검 9차(메인 SEO·접근성, 운영 빌드 16경로 × 데스크톱·375px 자동 점검) → AUTO-29 ✅ robots.txt·sitemap.xml, /features 영구 이동, 제목 단계 건너뜀. E2E 22 (web-seo 2건 추가 — 페이지 추가 시 sitemap 누락 검출) — AUDIT §16
- 재점검 10차(키보드 조작) → AUTO-30 ✅ 콘솔·메인 모달 Tab 가두기·포커스 복귀·첫 포커스·겹친 모달 Esc. 처음 만든 복귀 방식이 E2E 에서 실패(자식 autoFocus 가 먼저 포커스를 옮김) → 모달 밖 마지막 포커스 추적으로 수정. E2E 24 — AUDIT §17
- 재점검 11차(API 성능, notablescan 으로 전체 스캔 전수 검출) → AUTO-31 ✅ 로그인·가입이 users 전체 스캔하던 인덱스 결함 등 3건, AUTO-32 ✅ 리뷰 CSV 일괄 저장·재가져오기 중복 결함. API 테스트 상시 notablescan. API 68 · E2E 24 — AUDIT §18
  - 참고: 중복 판정 값이 바뀌어, 이 변경 **전에** 가져온 시트를 다시 가져오면 한 번은 중복될 수 있음 (운영 배포 전이라 영향 없음)
- 재점검 12차(운영 준비: 배포 문서대로 실제 수행) → AUTO-33 ✅ API Dockerfile 빌드 불가 결함(devDependencies 404·lockfile 미사용)·운영 시드 명령 오류 수정, CI api-image 잡 추가(이미지 빌드·운영 기동·시드·정리·기동 거부). 로컬 Docker 데몬이 꺼져 있어 이미지는 CI 로 확인 → 첫 실행 통과(빌드·헬스 200·시드·정리·기동 거부) — AUDIT §19
- 재점검 13차(콘솔 GitHub Pages 배포: 워크플로 ↔ out/ ↔ Pages 동작 흉내) → AUTO-34 ✅ 한국어 콘솔 404, 배포 변수 https 검사. E2E 24 — AUDIT §20
  - **책임님 확인 필요**: 콘솔 배포 워크플로는 테스트를 기다리지 않고 main push 로 바로 배포됨(빌드 실패만 막음) — CI 통과 후에만 배포하도록 묶을지(main 은 PR·CI 통과 후 머지하는 운영이면 지금대로 충분)
- 재점검 14차(web Vercel 배포) → AUTO-35 ✅ 필수 공개 변수 누락·http 시 Vercel 빌드 실패, 카카오 redirect 를 브라우저 주소로. 카카오 로그인 SDK 의 CSP 통과 확인(AUTO-17 의 확인 필요 항목 중 로그인 SDK 해소 — 지도 SDK 는 여전히 키 등록 후 확인). E2E 24 — AUDIT §21
- 재점검 15차(공용 계약) → AUTO-36 ✅ 응답 모양 ↔ 공용 스키마 자동 대조(읽기 API 14개, 불일치 0), 429 본문 requestId, 테스트 DB 기동 한도(간헐적 전체 건너뜀 해소). API 73 · E2E 24 — AUDIT §22
- 재점검 16차(새 clone 으로 README 따라 하기: 설치·env 없이 실행·7개 화면·모든 명령 성공) → AUTO-37 ✅ README 의 버전·개수·목록·시작 순서 교정 — AUDIT §23
  - **책임님 확인 필요**: README 끝 "This project is private and proprietary." 인데 레포는 공개(SECURITY R7) — 라이선스 문구를 둘지·레포를 비공개로 돌릴지
- AUTO-38 ✅ 결정 대기 16건을 **docs/DECISIONS.md** 로 모음 (급함 4 · 운영 정책 5 · 외부 서비스·도메인 4(카카오 키 등록 후 점검 포함) · 화면 내용 3). 이후 "책임님 확인 필요" 는 이 문서에 추가한다
