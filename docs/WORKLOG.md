# 작업 로그

지시사항·결정·상태를 날짜별로 누적한다. 기존 항목은 지우지 않는다.

## 2026-09-23

**지시사항 (예성빈 책임님)**
- 소스 정리: 화면별·기능별·페이지별·API별로, 환경변수까지 맞춰서 꼼꼼하게. 소스 전부 엎어도 됨
- 백엔드도 명확하게 확장·디벨롭
- 로그인 후엔 다른 URL: **메인 화면은 Vercel**, **기능 화면은 GitHub Actions 로 CI/CD**
- DB 는 일단 스키마만 — 모든 화면에 맞춰, 유저 DB 등 명확하게 관리
- 정합성 100% 기반, 오류 있으면 알려줄 것, 백로그 하나하나 설정해서 진행
- 토템 서포트 세션과 업무 분할
- 수정하면 항상 책임님 이름(yehsb123)으로 커밋·푸시 (AI 표시 금지)

**확인한 것**
- 공개 레포에 `totemBE/.env`(DB 접속정보·JWT 비밀키·TourAPI 키) 커밋, `node_modules` 8,776개 커밋
- FE 가 호출하는 API 경로 대부분이 BE 에 없음 → 로그인·코스 저장·투어·리뷰·설정 화면이 실제로 동작하지 않던 상태 (docs/AUDIT.md)

**결정**
- 모노레포 `apps/{web,console,api}` + `packages/shared` (API 계약 단일 출처)
- 도메인이 달라 localStorage 공유 불가 → 1회용 인계 코드(handoff)로 web → console 로그인 전달
- 조직(테넌트) 단위 데이터 격리, soft delete
- 작업 브랜치 `dev`

**진행**: 뼈대 재편 커밋 → PR #1 로 main 머지됨(책임님) → 구 배포 워크플로가 옛 경로로 실패

## 2026-09-25

**진행**
- API TypeScript 재작성 + 테스트 25건 통과, 실제 기동 스모크 테스트 통과
- 콘솔 6개 화면 재작성, 정적 export 빌드(basePath /totem) 통과, lint 경고 0
- 공용 클라이언트로 전 화면 흐름 E2E 17건 통과 (라이브 API)
- 메인(web)은 별도 작업 에이전트가 격리 워크트리에서 진행 (서포트 세션이 종료돼 있어 대체)
- CI(ci.yml) · 콘솔 배포(deploy-console.yml) 교체, ENV/API/DATABASE/DEPLOY 문서

**책임님 확인 필요**
- SEC-002 유출 키 재발급 (MongoDB 비밀번호, JWT, TourAPI, 카카오 REST)
- SEC-003 git 히스토리에서 .env 제거 여부 (force push 필요)
- OPS-004 API 배포처 결정
- 대시보드 관광소비 금액 단위 (원본이 원인지 천원인지 미검증 — 현재 "원" 표시)

## 2026-09-25 (계속)

- 책임님 `npm run dev` 실패(루트에 dev 스크립트 없음) → 루트 `npm run dev` 로 api·web·console 동시 실행 추가
- 로컬 `apps/api/.env` 의 옛 Atlas 주소가 해석되지 않아 api 가 죽던 문제 → 개발 모드에서만 인메모리 DB 로 대체(경고 출력), 운영은 기동 중단 유지. **자격증명 파일은 건드리지 않음**
- 메인(web) 재구성 브랜치 머지 (커밋 9개, 작성자 yehsb123, AI 표기 없음)
- 실제 브라우저 E2E 10/10, UI 결함 3건 수정 (입력칸 너비, 코스메이커 라벨, 통화 중복)

## 2026-09-25 (책임님 부재 — 자율 루프 시작)

**지시사항**: 1박 2일 여행 중 10분마다 루프로 원하는 결과물을 만들 것. 정합성 100%, 필요한 기능은 계속 추가, 묻지 말고 작업.
**진행**: npm audit 정리(vitest 5), 요금제 모델을 가격표와 일치(trial/basic/pro), 투어 일정표 PDF 구현, 투어 등록 시 달력 자동 표시,
인쇄 규칙 버그·달력 접근성 버그 수정, 메인 스크린샷 9장 실제 캡처로 교체, 가이드 문구 교정.
**자율 큐**: docs/BACKLOG.md §6
- AUTO-01 ✅ 브라우저 E2E 13건을 `e2e/`(Playwright)로 편입·CI 잡 추가. 과정에서 `next dev`/`next build` 가 같은 `.next` 를 공유해 빌드 시 개발 서버가 멈추는 결함 발견 → 개발 산출물 `.next-dev` 분리
- AUTO-02 ✅ 코스메이커 "내 코스"(검색·불러오기·일정표·삭제)·"새 코스". 투어가 연결된 코스 삭제는 409 로 막음(투어 링크 깨짐 방지), 목록에 tourCount. API 테스트 26 · E2E 14 (2회 연속 통과)
