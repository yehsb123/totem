import { HierarchyRectangularNode } from "d3-hierarchy";
interface TreeMapContext<TreeMapInputData> {
    width: number;
    height: number;
    data: TreeMapInputData;
    valuePropInData: string;
    paddingOuter: number;
    paddingInner: number;
}
export declare const calculateTreeMap: <TreeMapInputData>({ width, height, data, valuePropInData, paddingOuter, paddingInner, }: TreeMapContext<TreeMapInputData>) => HierarchyRectangularNode<TreeMapInputData>;
export {};
